__responsivePropsRender()
  if length(arguments) == 1
    font-size arguments
    
  else
    {shift(arguments)} arguments

sm()
  +moreThan(measures.breakpoints.sm)
    if arguments
      __responsivePropsRender arguments
    else
      {block}

md()
  +moreThan(measures.breakpoints.md)
    if arguments
      __responsivePropsRender arguments
    else
      {block}

lg()
  +moreThan(measures.breakpoints.lg)
    if arguments
      __responsivePropsRender arguments
    else
      {block}

xl()
  +moreThan(measures.breakpoints.xl)
    if arguments
      __responsivePropsRender arguments
    else
      {block}
